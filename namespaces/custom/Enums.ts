/**
 * 枚举
 */
namespace cs {
    /**
     * 日志类型
     */
    export enum LogTypes {
        REQUEST = "Request",
        NORMAL = "Normal"
    }

    /**
     * 本地存储 key
     */
    export enum LSTypes {
        UUID = "UUID",
        NORMAL = "Normal"
    }
}